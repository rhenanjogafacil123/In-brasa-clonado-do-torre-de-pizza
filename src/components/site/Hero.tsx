import { Bike,Clock,MapPin,ShoppingBag,Star,Store } from "lucide-react";
import { business } from "@/data/business";
import { whatsappLink } from "@/lib/whatsapp";
const heroImg="https://pizza-prime-digital.lovable.app/assets/hero-pizza-52s91XwR.jpg";
const serviceIcons=[Bike,ShoppingBag,Store];
export function Hero(){return <section id="topo" className="relative isolate min-h-[92svh] overflow-hidden"><img src={heroImg} alt="Pizza artesanal saindo do forno a lenha da Torre de Pizza" className="absolute inset-0 h-full w-full object-cover"/><div className="absolute inset-0 bg-gradient-hero"/><div className="relative mx-auto flex min-h-[92svh] max-w-6xl flex-col justify-end px-4 pb-14 pt-32 sm:px-6 md:pb-20"><div className="animate-rise max-w-2xl"><div className="mb-6 flex items-center gap-3"><span className="grid h-14 w-14 place-items-center rounded-2xl border border-gold/40 bg-primary-foreground/10 text-2xl backdrop-blur">🍕</span><div><p className="font-display text-lg font-semibold text-primary-foreground">{business.name}</p><p className="text-xs uppercase tracking-[0.22em] text-gold">Forno a lenha • desde sempre</p></div></div><h1 className="font-display text-4xl font-semibold leading-[1.05] text-primary-foreground sm:text-6xl">Sabor de verdade<span className="block text-gold">em cada fatia.</span></h1><p className="mt-4 max-w-lg text-base leading-relaxed text-primary-foreground/80 sm:text-lg">Massa artesanal, ingredientes frescos e o cheiro de forno a lenha que transforma qualquer noite em Campo Grande.</p><div className="mt-6 flex flex-wrap items-center gap-x-5 gap-y-2 text-sm text-primary-foreground/85"><span className="flex items-center gap-1.5 font-semibold text-gold"><Star className="h-4 w-4 fill-current"/>{business.rating.toLocaleString("pt-BR",{minimumFractionDigits:1})}<span className="font-normal text-primary-foreground/70">({business.reviews} avaliações)</span></span><span className="flex items-center gap-1.5"><MapPin className="h-4 w-4 text-gold"/>{business.city}</span><span className="flex items-center gap-1.5"><Clock className="h-4 w-4 text-gold"/>{business.hours}</span></div><div className="mt-5 flex flex-wrap gap-2">{business.services.map((s,i)=>{const Icon=serviceIcons[i]??Bike;return <span key={s} className="flex items-center gap-2 rounded-full border border-primary-foreground/20 bg-primary-foreground/10 px-4 py-1.5 text-xs font-medium text-primary-foreground backdrop-blur"><Icon className="h-3.5 w-3.5 text-gold"/>{s}</span>})}</div><div className="mt-8 flex flex-col gap-3 sm:flex-row"><a href="#cardapio" className="inline-flex items-center justify-center rounded-full bg-gradient-gold px-8 py-4 text-base font-semibold text-gold-foreground shadow-gold">Ver cardápio</a><a href={whatsappLink("Olá! Gostaria de fazer um pedido na Torre de Pizza.")} target="_blank" rel="noreferrer" className="inline-flex items-center justify-center rounded-full border border-primary-foreground/30 bg-primary-foreground/10 px-8 py-4 text-base font-semibold text-primary-foreground backdrop-blur">Pedir no WhatsApp</a></div></div></div><div className="absolute inset-x-0 bottom-0 h-24 bg-gradient-fade"/></section>}
